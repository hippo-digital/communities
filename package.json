{
  "private": true,
  "name": "communities",
  "version": "0.0.0",
  "description": "Internal website for Hippo communities",
  "scripts": {
    "start": "eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.2"
  }
}
